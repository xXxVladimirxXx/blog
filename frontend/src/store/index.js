import auth from './auth'
import general from './general'
import posts from './posts'
import users from './users'

export default {
    modules: {
        auth,
        general,
        posts,
        users
    }
}
